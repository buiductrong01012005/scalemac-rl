# ScaleMAC-RL v0.3.1

Documentation and repository-output organization update.

- Ignore ZIP archives with `*.zip` and `*.*zip`.
- Keep all tracked Markdown documentation under `docs/`.
- Write generated Markdown experiment reports to `docs/reports/`.
- Keep CSV results and checkpoints under `artifacts/`.
- Update package metadata to use `docs/README.md`.
